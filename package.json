{
  "name": "tedi-rtk-bridge",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Build tooling for the TEDI RTK Bridge extension. The shipped artifact is the bundled extension.js (built in CI into the release .zip, not committed); source lives in src/.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "check": "node --check extension.js && node scripts/verify.mjs && node scripts/verify-activate.mjs",
    "typecheck": "tsc -p jsconfig.json",
    "verify": "node scripts/verify.mjs && node scripts/verify-activate.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "typescript": "^5.9.0"
  }
}
